import cv2 as cv2
import numpy as np
import sys
import os
## code for testing Antenna,BMP, Camera
capture = cv2.VideoCapture(0)
capture.set(cv2.CAP_PROP_FRAME_WIDTH, 1920)
capture.set(cv2.CAP_PROP_FRAME_HEIGHT, 1080)
capture.set(cv2.CAP_PROP_FPS, 30)  # Requesting 

while True:
    ##insert BMP sensor code
    
    ##Antenna code
    
    ##review camera
    
    